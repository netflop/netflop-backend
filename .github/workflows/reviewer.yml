name: Add Reviewers

on:
  pull_request:
    types:
      - opened
      - reopened
      - closed  # for merge PR
      - synchronize
    branches:
      - 'develop'
      - 'master'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Add Reviewers
      uses: madrapps/add-reviewers@v1
      with:
        token: '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'
        reviewers: thanghuynh2608
        re-request-when-approved: true
        re-request-when-changes-requested: true
        debug-mode: true
